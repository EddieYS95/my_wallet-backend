version: "3.3"
services:
  web:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - db
  db:
    image: postgres
    environment:
      - POSTGRES_PASSWORD=gocl00!!
      - POSTGRES_USER=heachi
      - POSTGRES_DB=heachi
    ports:
      - "5432:5432"
